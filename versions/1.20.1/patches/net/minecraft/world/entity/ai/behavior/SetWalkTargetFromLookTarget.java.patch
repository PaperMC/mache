--- a/net/minecraft/world/entity/ai/behavior/SetWalkTargetFromLookTarget.java
+++ b/net/minecraft/world/entity/ai/behavior/SetWalkTargetFromLookTarget.java
@@ -15,7 +15,7 @@
 
     public static OneShot<LivingEntity> create(Predicate<LivingEntity> predicate, Function<LivingEntity, Float> speed, int completionRange) {
         return BehaviorBuilder.create(
-            context -> context.<MemoryAccessor, MemoryAccessor>group(
+            context -> context.group(
                         context.absent(MemoryModuleType.WALK_TARGET), context.present(MemoryModuleType.LOOK_TARGET)
                     )
                     .apply(context, (walkTarget, lookTarget) -> (world, entity, time) -> {
