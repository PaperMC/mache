--- a/net/minecraft/core/RegistrySetBuilder.java
+++ b/net/minecraft/core/RegistrySetBuilder.java
@@ -22,11 +22,11 @@
 public class RegistrySetBuilder {
     private final List<RegistrySetBuilder.RegistryStub<?>> entries = new ArrayList<>();
 
-    static <T> HolderGetter<T> wrapContextLookup(final HolderLookup.RegistryLookup<T> owner) {
-        return new RegistrySetBuilder.EmptyTagLookup<T>(owner) {
+    static <T> HolderGetter<T> wrapContextLookup(final HolderLookup.RegistryLookup<T> wrapper) {
+        return new RegistrySetBuilder.EmptyTagLookup<T>(wrapper) {
             @Override
             public Optional<Holder.Reference<T>> get(ResourceKey<T> resourceKey) {
-                return owner.get(resourceKey);
+                return wrapper.get(resourceKey);
             }
         };
     }
@@ -273,8 +273,8 @@
                 ResourceKey<?> resourceKey = entry.getKey();
                 if (resourceKey.isFor(this.key)) {
                     RegistrySetBuilder.RegisteredValue<T> registeredValue = (RegistrySetBuilder.RegisteredValue)entry.getValue();
-                    Holder.Reference<T> reference = state.lookup.holders.remove(resourceKey);
-                    map.put(resourceKey, new RegistrySetBuilder.ValueAndHolder<>(registeredValue, Optional.ofNullable(reference)));
+                    Holder.Reference<T> reference = (Holder.Reference<T>) state.lookup.holders.remove(resourceKey);
+                    map.put((ResourceKey<T>) resourceKey, new RegistrySetBuilder.ValueAndHolder<>(registeredValue, Optional.ofNullable(reference)));
                     iterator.remove();
                 }
             }
@@ -296,7 +296,7 @@
         }
 
         <T> Holder.Reference<T> getOrCreate(ResourceKey<T> key) {
-            return this.holders.computeIfAbsent(key, resourceKey -> Holder.Reference.createStandAlone(this.owner, resourceKey));
+            return (Holder.Reference<T>) this.holders.computeIfAbsent((ResourceKey<Object> ) key, resourceKey -> Holder.Reference.createStandAlone(this.owner, resourceKey));
         }
     }
 
