--- a/net/minecraft/network/protocol/game/ClientboundUpdateRecipesPacket.java
+++ b/net/minecraft/network/protocol/game/ClientboundUpdateRecipesPacket.java
@@ -46,6 +46,6 @@
     public static <T extends Recipe<?>> void toNetwork(FriendlyByteBuf buffer, T recipe) {
         buffer.writeResourceLocation(BuiltInRegistries.RECIPE_SERIALIZER.getKey(recipe.getSerializer()));
         buffer.writeResourceLocation(recipe.getId());
-        recipe.getSerializer().toNetwork(buffer, recipe);
+        ((net.minecraft.world.item.crafting.RecipeSerializer<T>) recipe.getSerializer()).toNetwork(buffer, recipe);
     }
 }
