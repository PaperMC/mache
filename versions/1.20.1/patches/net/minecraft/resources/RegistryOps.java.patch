--- a/net/minecraft/resources/RegistryOps.java
+++ b/net/minecraft/resources/RegistryOps.java
@@ -23,7 +23,7 @@
 
             @Override
             public <T> Optional<RegistryOps.RegistryInfo<T>> lookup(ResourceKey<? extends Registry<? extends T>> registryKey) {
-                return this.lookups.computeIfAbsent(registryKey, lookupProvider::lookup);
+                return (Optional<RegistryInfo<T>>) this.lookups.computeIfAbsent(registryKey, lookupProvider::lookup);
             }
         };
     }
