--- a/net/minecraft/resources/RegistryOps.java
+++ b/net/minecraft/resources/RegistryOps.java
@@ -23,7 +23,7 @@
 
             @Override
             public <T> Optional<RegistryOps.RegistryInfo<T>> lookup(ResourceKey<? extends Registry<? extends T>> resourceKey) {
-                return this.lookups.computeIfAbsent(resourceKey, lookupProvider::lookup);
+                return (Optional<RegistryInfo<T>>) this.lookups.computeIfAbsent(resourceKey, lookupProvider::lookup);
             }
         };
     }
