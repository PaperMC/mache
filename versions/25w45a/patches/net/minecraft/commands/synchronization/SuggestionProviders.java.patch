--- a/net/minecraft/commands/synchronization/SuggestionProviders.java
+++ b/net/minecraft/commands/synchronization/SuggestionProviders.java
@@ -42,7 +_,7 @@
         if (suggestionProvider1 != null) {
             throw new IllegalArgumentException("A command suggestion provider is already registered with the name '" + identifier + "'");
         } else {
-            return new SuggestionProviders.RegisteredSuggestion(identifier, suggestionProvider);
+            return (SuggestionProvider<S>) new SuggestionProviders.RegisteredSuggestion(identifier, suggestionProvider);
         }
     }
 
