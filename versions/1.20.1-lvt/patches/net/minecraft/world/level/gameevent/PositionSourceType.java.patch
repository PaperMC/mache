--- a/net/minecraft/world/level/gameevent/PositionSourceType.java
+++ b/net/minecraft/world/level/gameevent/PositionSourceType.java
@@ -30,6 +30,6 @@
 
     static <T extends PositionSource> void toNetwork(T positionSource1, FriendlyByteBuf buf) {
         buf.writeResourceLocation(BuiltInRegistries.POSITION_SOURCE_TYPE.getKey(positionSource1.getType()));
-        positionSource1.getType().write(buf, positionSource1);
+        ((PositionSourceType<T>) positionSource1.getType()).write(buf, positionSource1);
     }
 }
