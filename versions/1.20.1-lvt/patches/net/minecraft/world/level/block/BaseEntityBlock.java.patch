--- a/net/minecraft/world/level/block/BaseEntityBlock.java
+++ b/net/minecraft/world/level/block/BaseEntityBlock.java
@@ -38,6 +38,6 @@
     protected static <E extends BlockEntity, A extends BlockEntity> BlockEntityTicker<A> createTickerHelper(
         BlockEntityType<A> blockEntityType, BlockEntityType<E> expectedType, BlockEntityTicker<? super E> ticker
     ) {
-        return expectedType == blockEntityType ? ticker : null;
+        return expectedType == blockEntityType ? (BlockEntityTicker<A>) ticker : null;
     }
 }
